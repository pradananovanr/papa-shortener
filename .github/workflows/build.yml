name: Build & Deploy

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  ci-cd:
    uses: pradananovanr/papa-ci-cd-collection/.github/workflows/pipeline.yml@master
    secrets:
      DOKPLOY_WEBHOOK_URL: ${{ secrets.DOKPLOY_WEBHOOK_URL }}
